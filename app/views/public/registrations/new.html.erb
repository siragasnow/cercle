<div class="registration row">
  <div class="col-xs-6" style="text-align: center">
    <h2>ようこそ、<strong><span style="color:#3CB371;">cercle</span></strong> へ！</h2>
    <p>会員登録をして様々な給食を投稿しましょう！</p>
    <p>登録済みの方は<%= link_to "こちら", new_school_session_path %>からログインをしましょう！</p>
    <div class="sign-up-image">
    </div>
  </div>

  <div class="col-xs-4 col-xs-offset-1">
    <div class="sign-up-form">
      <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <%= render "public/shared/error_messages", resource: resource %>

        <div class="field">
          <%= f.label :学校名 %><br />
          <%= f.text_field :name, size: "30", :placeholder =>"例）〇〇〇〇学校" %>
        </div>

        <div class="field">
          <%= f.label :メールアドレス %><br />
          <%= f.email_field :email, size: "30", autofocus: true, autocomplete: "email", :placeholder =>"例）〇〇＠〇〇.〇〇" %>
        </div>

        <div class="field">
          <%= f.label :郵便番号 %><br />
          <%= f.text_field :postcode, size: "30", :placeholder =>"例）1234567" %>
        </div>

        <div class="field">
          <%= f.label :住所 %><br />
          <%= f.text_field :street_address, size: "30", :placeholder =>"例）〇〇県〇〇市〇〇０−０−０" %>
        </div>

        <div class="field">
          <%= f.label :電話番号 %><br />
          <%= f.text_field :phone_number, size: "30", :placeholder =>"例）０００１２３４５６７８" %>
        </div>

        <div class="field">
          <%= f.label :パスワード %>
          <% if @minimum_password_length %>
            <em>(<%= @minimum_password_length %> characters minimum)</em>
          <% end %><br />
          <%= f.password_field :password, size: "30", autocomplete: "new-password" %>
        </div>

        <div class="field">
          <%= f.label :確認用パスワード %><br />
          <%= f.password_field :password_confirmation, size: "30", autocomplete: "new-password" %>
        </div>

        <div class="actions">
          <%= f.submit "Sign up", class:"btn btn-primary" %>
        </div>
      <% end %>
    </div>
  </div>
</div>

